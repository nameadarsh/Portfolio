# Groq API key — server-side only (also set in Vercel project settings for production)
# Get a key at https://console.groq.com/keys
# No quotes, no VITE_ prefix — must never be exposed to the client
GROQ_API_KEY=

# Optional model override
# GROQ_MODEL=llama-3.3-70b-versatile
